import { PopulationFormatPipePipe } from './population-format-pipe.pipe';

describe('PopulationFormatPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PopulationFormatPipePipe();
    expect(pipe).toBeTruthy();
  });
});
